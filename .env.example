# Example environment variables
# Copy this file to .env and fill in the actual values

# === Global Forest Watch (opcional, si se usa en otros módulos) ===
GFW_USERNAME=your_username_here
GFW_PASSWORD=your_password_here
ALIAS=your_alias_here
EMAIL=your_email_here
ORG=your_org_here

# === Google Cloud Platform ===
# ID del proyecto de GCP con Earth Engine habilitado
GCP_PROJECT=your-gcp-project-id

# Ruta a las credenciales del service account de GCP
# Descarga el archivo JSON desde Google Cloud Console > IAM & Admin > Service Accounts
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# === Almacenamiento ===
# Bucket de GCS donde se guardan los reportes generados
OUTPUTS_BASE_PATH=gs://your-output-bucket

# Bucket de GCS donde están los inputs (AOIs, imágenes de header/footer)
INPUTS_PATH=gs://your-input-bucket

